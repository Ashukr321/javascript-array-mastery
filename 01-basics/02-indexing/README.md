# Array Indexing

JavaScript arrays are zero-indexed. The first element of an array is at index 0, the second at index 1, and so on.

```javascript
const numbers = [10, 20, 30];
console.log(numbers[0]); // Output: 10
```
