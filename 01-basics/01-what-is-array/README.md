# What is an Array?

An array is a special variable that can hold more than one value at a time. It is a fundamental data structure in JavaScript used to store ordered collections of data.

```javascript
const fruits = ['Apple', 'Banana', 'Orange'];
```
