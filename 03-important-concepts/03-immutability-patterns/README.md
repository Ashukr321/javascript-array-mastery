# immutability-patterns

 immutable means never change the original array , instead you always create the new array copy with the change applies 

<!-- Object.freeze();
hard immutability : 


real application : like shopping cart ui : 
addToCart 
removeToCart 
task manager 

⭐⭐ never mutate the original array data always return new array 

 search and filter ui :

 // array is mutable in nature : means we can mutate the original array : 
 some methods provided which mutate array directly 
 sort , reverse , splice , push , pop , shift , unShift,  fill , copyWithin ,  etc  

 <!-- there is some non mutating methods 
  every 
  filter 
  find 
  foreach 
  map 
  reduce
  some 
  with 
  toSplice 
  toSorted 
  -->
 -->

 <!--  polyfills : custom methods created by developer if the inbuilt methods not exits  -->