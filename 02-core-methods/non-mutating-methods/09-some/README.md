# some
some is the one of the widely  use js array methods it work like every methods : 
in every method when all element is satsify the condition then only give the true else false 

// but in this methods if one of the all array element will satisfy the condition then it will gives the true 
// and it does't even check further data element 
// it will stop early even it find one true ; not like every method 







