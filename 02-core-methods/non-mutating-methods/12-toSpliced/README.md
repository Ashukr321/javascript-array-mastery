# toSpliced

this methods is similar to splice methods : where splice method is the mutating methods that will mutate the original array 
// but toSplice methods does't mutate the original array this will return the new array after performing the operator 



