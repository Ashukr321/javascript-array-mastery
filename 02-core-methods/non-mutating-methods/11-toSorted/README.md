# toSorted
// toSorted methods is just similar to sort methods 
// sort methods is mutating methods that mutate the original array , 
//toSorted methods is the non mutating methods that will sort the array as like sort method do but 
// this will not mutate the original array and it will return the new sorted array 




