// Practice: toSorted

const arr = [3,2,3,4,5,5,1];
const sortedArr = arr.toSorted((a,b)=>b-a);

// compare function is the optional function , by this we can sort in ascending or descending order element
 
console.log(sortedArr);
console.log(arr);
